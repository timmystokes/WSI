import React from 'react';
import { shallow } from 'enzyme';
import ProductList from '../components/ProductList';


describe('<ProductList />', () => {
	it('I would add tests for ProductList component here', () => {
	});
});
